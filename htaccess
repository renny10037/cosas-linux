redirect http to https
  RewriteEngine On
  RewriteCond %{HTTP_HOST} ^dominio\.co [NC]
  RewriteCond %{SERVER_PORT} 80
  RewriteRule ^(.*)$ https://dominio/$1 [R,L]


bloquear navegación de directorio
  Options All -Indexes

para mas seguridad en wordpress vivitar:https://www.ebooz.com/trucos-con-htaccess-para-aumentar-la-seguridad-en-wordpress/
